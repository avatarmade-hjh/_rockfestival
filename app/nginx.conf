server {
    listen 80;
    server_name rockfestival.example.com;  # 실제 도메인으로 변경

    # HTTP → HTTPS 리다이렉트 (SSL 사용 시 활성화)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Next.js 정적 파일 캐싱
    location /_next/static/ {
        proxy_pass http://127.0.0.1:3000;
        expires 365d;
        add_header Cache-Control "public, immutable";
    }

    # public 폴더 정적 파일 캐싱
    location /images/ {
        proxy_pass http://127.0.0.1:3000;
        expires 30d;
        add_header Cache-Control "public";
    }

    location /fonts/ {
        proxy_pass http://127.0.0.1:3000;
        expires 365d;
        add_header Cache-Control "public, immutable";
    }

    # gzip 압축
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript image/svg+xml;
    gzip_min_length 1000;
}

# HTTPS 설정 (SSL 인증서 준비 후 활성화)
# server {
#     listen 443 ssl http2;
#     server_name rockfestival.example.com;
#
#     ssl_certificate /etc/letsencrypt/live/rockfestival.example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/rockfestival.example.com/privkey.pem;
#
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
#
#     location /_next/static/ {
#         proxy_pass http://127.0.0.1:3000;
#         expires 365d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location /images/ {
#         proxy_pass http://127.0.0.1:3000;
#         expires 30d;
#         add_header Cache-Control "public";
#     }
#
#     location /fonts/ {
#         proxy_pass http://127.0.0.1:3000;
#         expires 365d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     gzip on;
#     gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript image/svg+xml;
#     gzip_min_length 1000;
# }
